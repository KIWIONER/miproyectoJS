// DOM ( modelo de objetos del documento)
/*es una estructura de objetos generales por el navegador, 
la cual representa la pagina HTML actual.
en el modelo de objetos del documento (DOM), cada etiqueta HTML es un objeto, 
al que podemos llamar nodo. las etiquetas anidadas son nodos hijos de 
la etiqueta nodo padre que las contiene.

todos objetos son accesibles empleando JS mediante el objeto global document.
por ejemplo, document.body.
